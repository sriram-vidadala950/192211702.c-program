#include<stdio.h>
main()
{
	int rem,num,sum,temp;
	printf("\n enter a number : ");
	scanf("%d",&num);
	temp = num;
	int i = 0;
	while(num)
	{
		sum = sum+i+1;
	}
	printf("\n sum given digits %d is : %d",temp,sum);
	
}
